server:
  port: 8082
spring:
  application:
    name: ms-pedidos
  datasource:
    url: jdbc:postgresql://localhost:5432/smartlogix_orders
    username: postgres
    password: postgres_password
  jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
